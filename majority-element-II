class Solution:
    def majorityElement(self, nums: List[int]) -> List[int]:
        threshold =  (len(nums)//3)
        res = []
        seen = {}
        for num in nums:
            if num in seen:
                seen[num] += 1
                if seen[num] > threshold and num not in res:
                    res.append(num)
            else:
                seen[num] = 1
                if seen[num] > threshold and num not in res:
                    res.append(num)
        return res